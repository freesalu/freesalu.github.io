'use strict';

describe('myApp.freedomView module', function() {

  beforeEach(module('myApp.freedomView'));

  describe('freedomView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('FreedomViewCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});